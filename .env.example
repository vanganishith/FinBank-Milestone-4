# Values used by the Spring services when running locally.
DB_URL=jdbc:mysql://localhost:3306/bankdb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
DB_USERNAME=finbank
DB_PASSWORD=finbank
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
EUREKA_DEFAULT_ZONE=http://localhost:8761/eureka
JWT_SECRET=replace-this-with-a-long-random-secret-before-deployment
